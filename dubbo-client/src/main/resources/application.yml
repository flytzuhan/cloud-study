server:
  port: 8866
  servlet:
    context-path: /dubbo-client
spring:
  application:
    name: dubbo-client
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true